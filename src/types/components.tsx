export type FooterItem = {
    name: string,
    Icon: React.ElementType
    link: string
    route: boolean
}

export type NavBarItem = {
    name: string, 
    link: string,
    icon: React.ElementType
}